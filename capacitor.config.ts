import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chessreview.app',
  appName: 'Chess Review',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
